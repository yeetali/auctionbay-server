import { RegistrationInterceptor } from './registration.interceptor';

describe('RegistrationInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new RegistrationInterceptor()).toBeDefined();
  });
});
